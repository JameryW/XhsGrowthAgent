# P1b S3/S4 progress — 2026-09-20

Implemented the validated YAML context schema, safe placeholder substitution,
L0-L5 segmentation, stable-prefix message construction, required context
preservation and bounded token compilation in `backend/context/prompts.py`.

Added request-local `RunContext` construction at the artifact hydration seam.
Concurrent nodes receive independent immutable values, and the ContextVar is
reset on success or failure. Main prompt consumers now use compiled messages;
recall results retain provenance/ranking while exposing a transitional sequence
facade for older consumers.

Added prompt equivalence, stable-prefix, budget, fail-fast, schema and
concurrent-context tests in `tests/unit/context/test_prompts.py`. Blank niches
fail before prompt construction; historical notes without evidence render
`未提供赛道（不可推断）`.

`python -m compileall -q backend tests` passes. The project virtualenv runner
was rejected by the approval layer after the usage limit was reached, so full
pytest/mypy/Ruff gates must be rerun when execution is available. Git still
contains missing objects and has not been rewritten.

Remaining: rerun all gates, finish specialized prompt paths if equivalence tests
show drift, and add the deterministic S5 benchmark report.

S5 benchmark is now present as `s5-token-benchmark.py` and its deterministic
output as `s5-token-benchmark-2026-09-20.json`: 14 prompt files, mean legacy
estimate 575.36 tokens, maximum 1640, with the L0-L2 stable-prefix contract.
All specialized prompt consumers now pass through `_prompt_messages`; only the
final virtualenv test/lint/type gate remains blocked by the execution usage
limit.
