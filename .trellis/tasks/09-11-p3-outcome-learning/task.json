{
  "id": "p3-outcome-learning",
  "name": "p3-outcome-learning",
  "title": "sub: p3-outcome-learning",
  "description": "",
  "status": "planning",
  "dev_type": null,
  "scope": null,
  "package": null,
  "priority": "P2",
  "creator": "codex",
  "assignee": "codex",
  "createdAt": "2026-09-11",
  "completedAt": null,
  "branch": "feat/p3-s3-sync-weak-label-backfill",
  "base_branch": "main",
  "worktree_path": null,
  "commit": "24453cf9",
  "pr_url": "https://github.com/JameryW/XhsGrowthAgent/pull/626",
  "subtasks": [
    "S1 outcome-label seam: DELIVERED (feat/p3-s1-outcome-label-seam, 5e7b43b5). The five metric keys stop being a literal in the caller and become one exported contract (WEAK_LABEL_METRIC_KEYS + build_weak_label) that analyst calls; the always-empty back-fill now says so in prose instead of reading like working code. The gap itself is registered as a test that goes red when metrics start arriving, and every scanner in that test has a positive control because a claim whose value is zero cannot have one built in. 8 cases, mutation self-check 11/11.",
    "S2 link result from projection to fact: DELIVERED (feat/p3-s2-link-from-projection-to-fact, c27b2f1e). The request-scoped matching in analytics.py is now one pure function, and the link is finally written down. backend/services/publish_identity.py owns the whole rule: normalize_platform_post_id (moved verbatim out of analytics -- same semantics, one owner instead of a copy on each side) plus resolve_platform_links, which returns a frozen LinkResolution of index pairs so a caller applies the decision to its own rows. That is what makes the link recomputable: no I/O, no mutation, so the same inputs give the same answer inside a request or in a sync job. evaluator_samples gained a nullable platform_post_id on both schema paths (CREATE TABLE plus the idempotent ALTER), and record_publish_identity is the single writer, called from the publisher node's one exit -- the only place holding both the thread and the published id, with evaluator_gate -> publish_gate -> publisher guaranteeing the sample exists first. 26 cases in tests/unit/services/test_publish_identity.py. TWO TICKET PREMISES WERE REFUTED BY RECONNAISSANCE (prd.md section 11): adding the column is NOT necessary to join the two sides -- backfill_engagement keys on thread_id alone, and analyst's thread_id, publisher's workflow_thread_id and analytics' _extract_post_data all resolve to the same value -- so the column's real value is making S3's REVERSE lookup (note_id -> sample) an index hit; and the free path already persists weak labels (free.py's get_analytics back-fills evaluator_samples.engagement with no link_status gate), so the gap was never 'nothing persists', it was 'the workflow path has no deterministic source of real metrics'. ONE BUG THE REFACTOR INTRODUCED AND THE TESTS CAUGHT: LinkGroup.status's first draft returned unmatched for 'no workflow claim + several imported claims on that id', where the old code reached its ambiguous branch; it is now explicit and carries both a table row and a mutant (M03). Not fixed: publisher's write-side startswith('mock_') guard (changing the stored value would move the publish contract, and the read side normalizes before comparing), and get_analytics' ungated back-fill (a write-frequency question for S3/S4). GATES: ruff 542 / format 542 / mypy 217 / baseline drift / tool gate all green; full pytest 3612 passed, 3 skipped (+26). MUTATION SELF-CHECK 14/14 killed, step 0 OK, restore=OK.",
    "S3 close the edge: DELIVERED (feat/p3-s3-sync-weak-label-backfill, 24453cf9). The gap was never chicken-and-egg -- it was a missing producer. The only writer of evaluator_samples.engagement (backfill_engagement) was reached from analyst, which reads metrics out of a publish-time publish_result, a payload carrying identity and status only; so engagement stayed NULL, count_labeled_since stayed 0 and maybe_evolve never left below threshold on a system that was fully wired and fully green. The creator-stats import knows both the post id and its real counts, so the seam is closed from that end. evaluator_config gains a shared clause (_ATTACH_WEAK_LABEL_SQL) plus ENGAGEMENT_LABEL_SOURCE, so the two writers differ only in WHICH ROWS they pick, never in WHAT they write; backfill_engagement_for_posts selects by platform_post_id, runs one transaction and returns the summed rowcount -- deliberately not the number of posts, since most imported notes were never judged. backfill_engagement also rewrites provenance, which is a correction rather than new semantics: label_source='evaluator' was already false in the database, because insert_sample writes it at judgement time and engagement arrives in a later UPDATE. pipeline wires the attach at import_bundle's single exit, after persist; the post id is normalized producer-side (normalize_platform_post_id stays the one owner) and the payload goes through build_weak_label (one declaration, shared with analyst). 27 cases. MUTATION SELF-CHECK FOUND AND FIXED A REAL HOLE: the end-to-end fixture read the expected label from the import instead of parsing it out of the statement it was executing, so dropping the label from the shared clause -- or writing the wrong value -- left the whole 'real write -> real counter -> evolved' chain green; M1 was only 3 red, all of them db-layer or keyword assertions. After the fixture parses the label out of the SQL: M1 -> 8 red (5 from the end-to-end file), M2 -> 4 red. That failure shape is worth naming: a fixture answering FOR the statement under test is a third variant of 'structurally complete, never started' (the first two being a column with no writer and a branch with no producer). TWO TICKET PREMISES REFUTED (prd.md section 13): the sync side has no consumer of the link result, so this slice builds a producer rather than using one; and label_source had ZERO readers before this slice (fetch_labeled_samples and count_labeled_since both filter on engagement IS NOT NULL and never look at it), so the only observable change here is the count and the evolution -- label_source=engagement is a key the ticket imagined for downstream, not something this slice makes any behaviour depend on. Check 4 of the S1 contract test did NOT go red, and that is correct: the sync path never builds a publish result. REGISTERED, NOT FIXED: the refit window is the sample's creation time, so a backfill can label an old row successfully and move the counter by zero (case 8); the free path's ungated backfill is a write-condition question (section 11); and the ordering comment on _attach_real_weak_labels is an intent claim, not a guaranteed property -- the tests cannot tell the two orders apart.",
    "S4 contract and the gate condition: docs/outcome-learning.md -- the definition of offline quality vs online reward, the write condition for the weak label, maybe_evolve's gate condition, and what this ticket deliberately does not do."
  ],
  "children": [],
  "parent": "09-11-runtime-upgrade",
  "relatedFiles": [
    "backend/db/evaluator_config.py",
    "backend/db/creator_stats.py",
    "backend/agents/analyst.py",
    "backend/agents/publisher.py",
    "backend/agents/nodes/evaluator.py",
    "backend/graph/builder.py",
    "backend/services/creator_stats/pipeline.py",
    "backend/api/routes/analytics.py",
    "backend/api/routes/free.py",
    "backend/api/routes/evaluation.py",
    "backend/memory/creator_agent_observations.py",
    "backend/creator_agent/observations.py",
    "backend/creator_agent/advisor.py",
    "backend/db/creator_agent.py",
    "tests/unit/db/test_evaluator_config.py",
    "tests/unit/api/test_analytics_identity.py",
    "tests/unit/api/test_creator_stats_scheduler.py",
    "tests/unit/creator_agent/test_evaluator_config.py",
    "docs/outcome-learning.md",
    ".trellis/tasks/09-11-p3-outcome-learning/prd.md",
    "tests/unit/db/test_weak_label_contract.py",
    "backend/services/publish_identity.py",
    "tests/unit/services/test_publish_identity.py",
    "tests/unit/services/creator_stats/test_weak_label_backfill.py",
    "backend/agents/nodes/publisher.py"
  ],
  "notes": "P3 PLANNING ROUND (feat/p3-s1-outcome-label-seam, commit a3dbb03c) -- reconnaissance and slice plan, no code yet. The ticket was a blank placeholder (status planning, subtasks [], no prd.md, both jsonl files holding only the example line), so this round produced prd.md with thirty-odd file:line findings, a rewritten framing, four slices, the red lines, the refusals and four open rulings. THE LOAD-BEARING FINDING: the parent ticket lists four things and THREE OF THEM ALREADY EXIST -- offline quality vs online reward is already distinguished in storage (evaluator_samples.label_source is declared as \"evaluator\" | \"engagement\" | \"human_review\", evaluator_config.py:347, with engagement as its own JSONB column), the online reward is already collected for real (creator_note_stats in db/creator_stats.py:70-91 carries views/likes/comments/collects/shares and its own engagement_rate, fed by a 6225-line services/creator_stats/ subsystem with a 1576-line CDP client), LearningSignal -> ModelRevision already has a full lifecycle, and the two personalization stacks ALREADY SHARE A READ SEAM (memory/creator_agent_observations.py:111 implements the CreatorContentObservationSource protocol declared in creator_agent/observations.py:16, consumed by creator_agent/advisor.py:177). Nothing needs to be built. WHAT IS ACTUALLY MISSING IS TWO CONNECTIONS, and both are proven, not inferred: (1) THE WEAK-LABEL FEED PATH DOES NOT EXIST. The only regular writer of evaluator_samples.engagement is agents/analyst.py:271-280, and its dict comprehension filters five keys (views/likes/collects/comments/shares) that publisher.py NEVER writes -- grep for those literals in publisher.py returns ZERO hits, and all twelve places that assign into publish_result carry error/error_type/recovery/result_known/note/publish_id metadata only. The gate is therefore always false. CORRECTED ON S1 DAY 1 (see prd.md section 9): the auto edge was removed, but analyst is NOT unreachable -- builder.py:187 continues 'It can be reached by resuming the workflow with phase=analyzing', state/modes.py:170/188 both map ANALYZING -> analyst, and api/routes/_wf_application.py:1818 is a complete manual trigger endpoint (assert_thread_owned + a has_analytics idempotence gate + Command(goto=[\"analyst\"])). So the break is NOT 'nobody runs it', it is 'running it still cannot work': publish_result carries none of the five keys, and the manual path reads the same publish-time dict. Corroboration: label_source=\"engagement\" appears ZERO times in backend/. (2) THE REAL METRICS NEVER REACH THE LEARNER. analytics.py contains ZERO references to evaluator_config / backfill_engagement / evaluator_samples / insert_sample, and the linker's product (link_status=\"linked\", written only at analytics.py:894 and :908) is a REQUEST-TIME PROJECTION that is never persisted. evaluator_samples has no platform_post_id column while backfill_engagement(thread_id, ...) keys on thread_id -- so even a willing caller has no key to join on. CONSEQUENCE (assertable): count_labeled_since counts engagement IS NOT NULL, so it is identically 0, which is below MIN_EVOLVE_SAMPLES = 10 (evaluator_config.py:896), so maybe_evolve never leaves the below-threshold branch (evaluator_config.py:976-977) and train_weights / PromptEpoch never advance. THE WHOLE LOOP IS STRUCTURALLY COMPLETE, FULLY TESTED AND GREEN, AND HAS NEVER RUN ONCE -- the purest member of the 'declaration is not execution' family: evaluator_config.py's module docstring promises 'eventually trained from real engagement feedback', analyst.py:263 promises a 'weak label for grader finetuning', publisher.py:351 promises an explicit id match -- three declarations, zero data points. REFRAMING (same shape as P2c's 'not a missing Planner, a missing plan object'): P3 is not missing an Outcome Learning loop, it is missing the two wires that were designed into it. Consequence for slicing: no new module, no new Agent, no cross-module refactor -- each slice only has to make one existing wire actually carry current. SLICES: S1 make the situation visible and stop the bleeding (turn 'the loop never started' into a gate that goes red, and deal with analyst.py's always-empty back-fill, today's biggest source of misinformation). S2 lift the linker out of request-time projection into a reusable form and give evaluator_samples a nullable platform_post_id. S3 close the wire: after creator-stats/sync, back-fill real metrics into evaluator_samples.engagement with label_source=\"engagement\", the first moment maybe_evolve is reachable. S4 the contract and gate condition document (docs/outcome-learning.md). S1's gate must distinguish 'never started' from 'ran and produced zero' -- those two are identical on all real data today (the same family as P2c-S5's 'a claim whose value is zero has no built-in control'), so it has to point at somewhere the thing DOES exist. OPEN RULINGS (all four carry the measured fact that decides them): (1) the parent's 'unify the two personalization stacks into a Memory Fabric' -- the READ side is already unified and no call site today reads both sides, so the write side is NOT consolidated; (2) merging decision-side LearningSignal (feedback-driven, pending_creator_review = human-gated) with engagement weak labels (automatic) would change the human gate's semantics, so NOT merged until a conflicting-conclusion instance exists; (3) whether creator_note_stats should re-drive sample recomputation -- deferred until S3 shows the re-back-fill cost (the table is an upsert and notes keep accruing); (4) MOST IMPORTANT -- what happens to the analyst node now that it is unreachable, since all three of its jobs (content_history metrics, weak-label back-fill, insights) silently stop happening when it does not run, while S1 must answer whether to restore its trigger or hand its duties to capability that already exists elsewhere. ANCHOR DISCIPLINE: every file:line claim in prd.md was programmatically re-checked after writing (51 anchors, each asserting an expected token on the cited line); the first pass caught 10 anchors that had drifted by 1-5 lines, all corrected before commit, and the document's stated counts (6225 / 1576 / 0 / 0 / 0) are re-derived by the same checker rather than copied.\n\n--- S1 DELIVERED (PR #624, feat/p3-s1-outcome-label-seam, commit 5e7b43b57e536303b420160b79013f707219daf6) ---\n\nWHAT LANDED: the weak-label ingest contract is declared once. backend/db/evaluator_config.py\nexports WEAK_LABEL_METRIC_KEYS (Final[tuple[str, ...]]) plus build_weak_label(payload), which\nselects exactly the keys a payload carries; backend/agents/analyst.py calls it instead of\nrepeating the five keys. Behaviour is unchanged to the character -- `if k in payload` makes the\nold `get(k, 0)` default unreachable -- and the misleading comment at the call site now states\nthe measurement instead of reading like working code.\n\nTHE GUARD: tests/unit/db/test_weak_label_contract.py (8 cases, 376 lines). The load-bearing one\nregisters the gap: the keys publish_result can carry (union of four AST shapes -- name-bound\ndict, dict under the \"publish_result\" key, dict literal passed to _with_publish_link_metadata,\nsubscript writes; 15 keys today) do not intersect the contract, and the shortfall is asserted\nto be all five. When the sync path starts writing metrics that test goes red on purpose.\n\nEVERY SCANNER HAS A CONTROL, because a claim whose value is zero (checks 1 and 4) cannot have\none built in: gutting a scanner to `return set()` keeps it green. The controls are what make the\ngap a measurement: the producer scan must still see post_id/post_url/status/publish_id plus the\nthree identity keys of the real _with_publish_link_metadata; a synthetic producer that does\nwrite views/likes/comments/shares must be visible; the formula scanner must see a different key\nset (impressions/saves); and label_source has exactly one writer, \"evaluator\", read as an AST\nkeyword argument so the comment in evaluator_config naming label_source=\"engagement\" cannot\nsatisfy the scan.\n\nWHAT S1 DID NOT DO (prd.md section 10): the \"build a sample row with engagement and assert\nmaybe_evolve returns evolved\" door from section 4 is NOT in S1, because such a row cannot be\nbuilt today -- backfill_engagement is only reached through the always-false `if engagement:`,\nand building it needs real metrics, which is S3. Registered as S3's entry condition.\n\nGATES: ruff check 540 files / ruff format --check 540 files / mypy 216 files / baseline drift /\ntool_runtime_gate all green; full pytest 3586 passed, 3 skipped (+8). Mutation self-check 11/11\nkilled, restore=OK.\n\nLEAD FOR S2/S3: publish_result already carries platform_post_id (and workflow_thread_id,\nlink_status) inside the checkpoint, so the join key the learning layer lacks exists on the state\nside. The missing key is only on evaluator_samples (no platform_post_id column, while\nbackfill_engagement(thread_id, ...) takes thread_id). S2 reconnaissance should compare (a)\nadding platform_post_id to evaluator_samples against (b) joining through thread_id -> checkpoint\n-> platform_post_id, instead of assuming (a).\n\n\n--- S2 DELIVERED (feat/p3-s2-link-from-projection-to-fact, commit c27b2f1e) ---\n\nONE OWNER FOR PLATFORM IDENTITY: backend/services/publish_identity.py now holds the whole\nrule (normalize_platform_post_id + resolve_platform_links + the frozen LinkGroup /\nLinkResolution). analytics.py deleted its private _normalize_platform_post_id and imports the\npublic one at all three call sites; _merge_imported_posts no longer decides anything inline --\nit asks the resolver and applies the answer. The rule used to exist twice (analytics rejected\nmock_/workflow:, publisher rejected only mock_) and stayed in agreement only because the read\nside happened to normalize before comparing.\n\nRECOMPUTABLE, NOT LUCKY: the old matcher mutated the rows it was handed, so repeating it\nworked by accident of the current field set (the decision reads nothing but\nplatform_post_id, and none of the mutated fields is an input). The resolver is pure, and\nthere is a test that says so -- same inputs twice, equal results, inputs untouched -- with a\nnon-vacuity assertion in the same case so a resolver gutted to 'no links' cannot pass it.\n\nTHE LINK IS NOW A FACT: evaluator_samples.platform_post_id is nullable and created on both\npaths (CREATE TABLE for new installs, ADD COLUMN IF NOT EXISTS for upgrades);\nrecord_publish_identity updates the latest sample by thread and is called from the publisher\nnode's single exit, where thread_id and platform_post_id coexist and where the evaluator gate\nhas already run. A dry run normalizes to an empty id and writes nothing. This is the part\nthat keeps the slice from repeating the parent ticket's own disease -- a nullable column\nnobody fills would have been structure without a run, which is exactly what P3 exists to\nexpose.\n\nGATES: ruff check 542 files / ruff format --check 542 files / mypy 217 files / baseline drift\nwithin threshold / tool_runtime_gate OK; full pytest 3612 passed, 3 skipped (+26). Mutation\nself-check 14/14 killed, step 0 OK, restore=OK.\n\nLEAD FOR S3: samples now carry the published note id, so the sync back-fill can find its rows\nwith WHERE platform_post_id = ANY(...) instead of replaying checkpoints. Note that\nupsert_note_stats is an upsert and notes keep accruing, so S3's first version should back-fill\nonce per sync and leave incremental recomputation until there is evidence about the cost.\n --- S3 DELIVERED (feat/p3-s3-sync-weak-label-backfill, 24453cf9) --- THE GAP WAS A MISSING PRODUCER, NOT A MISSING KEY. Reconnaissance refuted the ticket's own wording (prd.md section 13): the sync side has no consumer of the link result, so S3 had to build a producer; and the metric pairing S3 was thought to lack already exists at api/routes/analytics.py:771-809, which builds {id, platform_post_id, likes, comments, collects, shares, views} out of NoteStats and normalizes the id at :785 -- data, keys and rule were all present, only the UPDATE was missing. THE LANDING SPOT IS FORCED, NOT CHOSEN: all three sync entries (pipeline.py sync_from_payload:571, sync_from_creator_center:757, sync_account_stats:839) funnel into import_bundle, whose persist_bundle:448 caller is unique, so wiring there and nowhere else is what makes the coverage total. label_source HAD ZERO READERS: the training pool (fetch_labeled_samples, evaluator_config.py:726, filtered at :806/:811) and count_labeled_since (:986, :1068/:1074) both select on engagement IS NOT NULL and never consult the column, so writing engagement alone is what makes this slice observable; label_source=engagement is a key the ticket imagined for downstream. Writing it is nonetheless a CORRECTION, not new semantics: insert_sample (:367) writes 'evaluator' at judgement time while engagement arrives in a separate UPDATE, so the stored value was already false. THE MOST VALUABLE THING THIS SLICE PRODUCED IS A FIXED HOLE IN ITS OWN TESTS: the end-to-end fixture imported ENGAGEMENT_LABEL_SOURCE and wrote it into its fake rows, answering FOR the production statement instead of executing it. Dropping the label from the shared clause (M1) or writing the wrong value (M2) therefore left the real-write -> real-counter -> evolved chain fully green -- 3 red each, all on db-layer and keyword assertions. Parsing the label out of the SQL (the standard the same fixture's _count already met, reading 'engagement IS NOT NULL' out of its predicate) moved M1 to 8 red with 5 from the end-to-end file and M2 to 4 red, and the mutation harness now asserts WHERE each of those two must be caught, so the hardening is pinned by the tool rather than by one lucky run. THAT IS THE THIRD VARIANT OF 'STRUCTURALLY COMPLETE, NEVER STARTED': a column with no writer, a branch with no producer, and now a fixture answering for the statement. The ticket's stated entry condition is case 6 of the new file and both ends are real -- the rows are written by the production path under test and counted by the production count_labeled_since; only the refit itself is stubbed, because a mocked counter would be green whether or not anything was ever written, which is exactly the failure this ticket exists to prevent. CHECK 4 OF THE S1 CONTRACT TEST DID NOT GO RED AND THAT IS CORRECT: it asserts that publish_result carries no metric key, and the sync path never builds a publish result. REGISTERED, NOT FIXED: count_labeled_since windows on the SAMPLE's creation time, so an old successfully-labeled row moves the counter by zero (case 8, S4's write-condition question); the free path's ungated backfill (free.py:1060-1072) is the same question; upsert_note_stats is an upsert so this first version back-fills once per sync, with incremental recomputation left to evidence; and the ordering comment on _attach_real_weak_labels is an intent claim the tests cannot falsify, since a failed persist blocks the attach in either order. 11 mutations, 11/11 killed.",
  "meta": {}
}
