# Public UX acceptance snapshot — 2026-08-10

This is a read-only acceptance record for the public Showcase and Replay surfaces.
No workflow was started and no LLM/provider call was made during this snapshot.

## Code and local verification

- `frontend/src/views/WorkflowReplay.vue`: the primary `#replay-results` grid no longer uses
  `v-reveal`; a tall result grid must not remain transparent while only a small slice intersects
  the mobile viewport.
- Step-detail failures now use the shared `ErrorState` presentational component.
- `npm run test:run -- --reporter=dot --silent`: 64 files / 676 tests passed.
- `npm run type-check`: passed.
- `npm run i18n:check`: 2130 keys consistent.
- `npm run build`: passed; no new >500KB Vite warning.

## Browser evidence

The local fixed build was served through Vite with the API proxy pointed at the deployed API and
opened with Chromium at 390×844, `zh-CN`, light theme. The first result title was visible without
scrolling; screenshot: `/tmp/public-ux-replay-probe.png`.

A dedicated manual pass also produced 12 screenshots (Showcase/Replay × 390/768/1440 × light/dark)
and recorded the Replay heading as `普通人搞钱与副业逻辑` in every viewport:
`/tmp/public-ux-manual-fixed-2026-08-10/manifest.json`.

The full local public matrix produced 95 page records with zero axe serious/critical violations.
Its warm-navigation p75 is not a production measurement because Vite development-server overhead
was included (968ms versus the 500ms budget). The corresponding deployed-container report before
this source-only change had 96 records, axe serious/critical=0, warm p75=383.45ms, cached
select-to-render p75=13.82ms, and zero performance-budget failures.

## Deployed read-only sample

The deployed `xhs-growth` container had `XHS_LATENCY_LOG=1`, `RIPPLE_BACKGROUND=1`, Ripple defaults
`max_waves=3`, `simulation_horizon=12h`, `ensemble_runs=1`, and local BGE embeddings enabled.

`collect_latency.py --container xhs-growth --since 30m` reported:

| Endpoint | n | p50 | p95 | avg | segment p50 |
| --- | ---: | ---: | ---: | ---: | --- |
| `/list` | 12 | 0.7ms | 2.1ms | 3.2ms | db 0.7ms; serialize 0.0ms |
| `/status` | 2 | 3.0ms | 3.0ms | 3.0ms | aget_state 1.4ms; db 0.5ms; serialize 0.2ms |

Fifty service-token GETs for one existing completed workflow status all returned HTTP 200. The
Postgres `workflows` table update counters stayed `n_tup_upd,n_tup_hot_upd = 0,0 → 0,0`.
This is evidence of the deployed optimized same-state path, not a historical old-version baseline.

`/api/health` returned `status=ok`; database, memory store, Ripple CAS, risk-control, search API,
and configured provider checks were healthy. The database currently has 2 completed workflows and
71 checkpoints; actual checkpoint `channel_values.performance_log` entries are 0.

## Release gates still requiring external confirmation

- A historical before sample for Direction 1's polling writes.
- Real LLM token/latency and content-quality before/after samples, plus a real provider call for the
  lighter-model routes.
- A unified production reliability/runtime baseline for Directions 3/4.
- Strict empty-state verification requires a target environment with no approved public case.
- Product/operations sign-off for funnel telemetry and the publisher's Lighthouse/manual screenshot
  archive at 390×844, 768×1024, and 1440×900 in both themes.
